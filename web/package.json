{
  "name": "disk-tool-web",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:demo": "tsc && vite build --mode demo",
    "preview": "vite preview",
    "e2e": "npx cypress@14.3.2 run",
    "e2e:open": "npx cypress@14.3.2 open"
  },
  "dependencies": {
    "echarts": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^6.4.3"
  }
}
