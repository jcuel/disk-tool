# Proposal: Remove template/ bootstrap folder

**Change:** remove-template-folder
**Status:** in progress

## Summary

Remove the obsolete `template/` SPECBOOT bootstrap copy. disk-tool keeps its own `.cursor/`, `openspec/`, and `.github/` at repo root.

## Scope

- Delete `template/` (24 files)
- Remove README "Reuse for new projects" section
- Update `.github/PROJECT.md` milestone note
