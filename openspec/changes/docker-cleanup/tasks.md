# Tasks: Docker cleanup

- [x] OpenSpec package + GitHub issue + `feat/docker-cleanup` branch
- [x] `internal/docker` detect/df/prune/paths + unit tests
- [x] `CategoryDocker` + insights + `docker-reclaim` preset + FS protect data roots
- [x] API GET/POST docker + UI preset modal
- [x] testing-report.md + PR to `dev`
