# Tasks: Tauri desktop shell

**Change:** `desktop-shell`

## Implementation Checklist

- [x] Create feature branch `cursor/tauri-desktop-shell-5b79`
- [x] OpenSpec artifacts (user-story, proposal, design, tasks, delta spec)
- [x] Go sidecar hardening (`--port 0`, `--ready-stdout`, graceful shutdown)
- [x] Tauri 2 scaffold under `desktop/` with sidecar lifecycle
- [x] Build scripts (`build-desktop.sh`, `build.ps1`, Makefile)
- [x] CI sidecar smoke + release tauri-action
- [x] README desktop install section
- [x] Update testing report

## Notes

Branch: `cursor/tauri-desktop-shell-5b79`. Target PR base: `dev`.
